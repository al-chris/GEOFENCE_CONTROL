[project]
name = "mikun"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "al-chris", email = "christopheraliu07@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "lgpio>=0.2.2.0",
]

[project.scripts]
mikun = "mikun:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
